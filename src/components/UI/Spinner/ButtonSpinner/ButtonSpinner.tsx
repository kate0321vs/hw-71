import { CircularProgress } from '@mui/material';

const ButtonSpinner = () => {
    return (
        <>
            <CircularProgress size={25} />
        </>
    );
};

export default ButtonSpinner;